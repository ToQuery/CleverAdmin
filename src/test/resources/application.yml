
spring:
  datasource:
    url: jdbc:mysql://mysql.jszx.sparke.cn:3306/cleverweb?autoReconnect=true&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true
    username: root
    password: spark2016
  ldap:
    embedded:
      ldif: ldap-server.ldif
      base-dn: dc=didispace,dc=com
server:
  port: 8899

logging:
  level:
    root: info