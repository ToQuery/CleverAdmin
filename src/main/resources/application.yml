app:
  security:
    whitelist:
      - /
      - /index.html
      - /static/**
      - /error
      - /h2-console/**
      - /app/ueditor
      - /app/files
      - /app/files/**
  jwt:
    secret: 123456
  page:
    param:
      page-size: ps


spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev